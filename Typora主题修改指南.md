# Typora 主题自定义修改指南

> 适用文件：`github-pro-light.css`、`github-pro-dark.css`、`base.user.css`（2026-09-04 交付版）
> 文中行号以该版本为准；你自己改动后行号会偏移，**建议优先用"搜索关键词"定位**（第 5 节有方法）。

---

## 目录

1. [修改前的准备](#1-修改前的准备)
2. [三分钟看懂要改的 CSS](#2-三分钟看懂要改的-css)
3. [三个文件的分工与加载顺序](#3-三个文件的分工与加载顺序)
4. [常用修改速查](#4-常用修改速查)
5. [怎么自己找到要改的位置](#5-怎么自己找到要改的位置)
6. [常见坑：改了没生效 / 改坏了](#6-常见坑)
7. [行号总索引](#7-行号总索引)
8. [改坏了怎么恢复](#8-改坏了怎么恢复)

---

## 1. 修改前的准备

1. **找到主题目录**：Typora → `文件` → `偏好设置` → `外观` → `打开主题文件夹`。
   你的三个文件就放在这里（`github-pro-dark.css` 依赖的 `github/base.css` 也在其 `github/` 子文件夹里，**不要动它**）。
2. **先备份**：把整个主题文件夹复制一份，改名 `backup-日期/`。CSS 改坏最严重的后果就是"样式乱掉"，有备份随时能回退。
3. **用合适的编辑器**：VS Code、Notepad++ 都可以。保证保存为 **UTF-8 编码**（默认就是）。
4. **让修改生效**：保存文件后回到 Typora 按 `Ctrl + R`（重新加载），或者切换到别的主题再切回来。不需要重启 Typora。
5. **建议的修改节奏**：一次只改一个地方 → 保存 → `Ctrl + R` 看效果。一次改十个地方再刷新，出了问题不知道是谁引起的。

---

## 2. 三分钟看懂要改的 CSS

一条 CSS 规则长这样：

```css
选择器 {
  属性: 值;   /* ← 你要改的基本都是"值" */
}
```

```css
.md-fences {            /* 选择器：代码块（. 开头 = 类名） */
  border-radius: 12px;  /* 属性: 圆角 12 像素。改成 0 就是直角 */
}
```

只需要认识下面这几样东西：

| 写法 | 含义 | 例子 |
|---|---|---|
| `#write` | `#` 开头 = ID 选择器，指编辑区（正文） | `#write { max-width: 85% }` |
| `.md-fences` | `.` 开头 = 类名。常见：代码块 `.md-fences`、目录 `.md-toc`、表格 `table`、引用 `blockquote`、公式块 `.mathjax-block` | |
| `xxx::before` | 伪元素：在元素前面"画"出来的东西。**三个小圆点就是 `::before` 画的** | `.md-fences::before` |
| `var(--xxx)` | 使用 CSS 变量。变量的定义都在文件开头 `:root { ... }` 里 | `border-radius: var(--gp-radius-l)` |
| `px / em / rem` | 像素 / 相对当前字号 / 相对根字号。改整体字号时，em/rem 的内容会自动跟着缩放 | |
| `!important` | 强制优先。**能不用就不用**（见第 6 节的教训） | |

**优先级口诀**：`!important` > 行内样式 > `#id` > `.class`；同等优先级时，**写在后面的赢**。这就是为什么 `base.user.css`（最后加载）能覆盖主题里的设置。

**注释**：`/* 这是注释 */`，可以随便加，用来给自己记笔记：

```css
border-radius: 12px;  /* 2026-09-05 我把它从 10px 改成了 12px */
```

---

## 3. 三个文件的分工与加载顺序

| 文件 | 管什么 | 颜色/字号定义在哪 |
|---|---|---|
| `github-pro-light.css` | 浅色主题，完整独立 | 集中在文件开头 `:root` 的 `--gp-*` 变量（改变量 = 全局生效） |
| `github-pro-dark.css` | 深色主题（依赖 `./github/base.css`） | 部分在 `:root` 变量（19~55 行），部分是硬编码色值（要逐处找） |
| `base.user.css` | **两主题通用**的全局配置：字体方案、代码字号、编辑区宽度 | 集中在 `:root` 的 `--user-font-*` 四个变量 |

**加载顺序**：`github/base.css` → 主题 css → `base.user.css`（最后加载）。
所以：**字体、宽度这类"跨主题"的设置放 base.user.css；纯颜色的设置放对应主题文件。**

---

## 4. 常用修改速查

> 每项格式：**改什么 → 在哪个文件 → 搜什么 → 现在的值 → 怎么改**。

### 4.1 中英文字体（正文宋体 / 英文 Source Code Pro）

- **文件**：`base.user.css`，`:root` 里 **21~33 行**，四个变量：

```css
--user-font-body-latin: "Source Code Pro", ...;  /* 正文英文/数字 */
--user-font-body-han:  "SimSun", "宋体", ...;    /* 正文中文（默认宋体） */
--user-font-code-latin: "Source Code Pro", ...;  /* 代码块/行内代码英文 */
--user-font-code-han:  "Microsoft YaHei", ...;   /* 代码里的中文 */
```

- **改法**：把想用的字体名填到对应变量的**第一个位置**，如 `"霞鹜文楷 GB"`、`"Times New Roman"`。名字要带引号，用逗号分隔多个候选（前面没有的字体自动用后面的，叫"回退"）。
- 字体必须**已安装在你电脑上**才会生效。字体名可在"控制面板 → 字体"里查。
- 原理（想了解再看）：浏览器对每个字符按字体栈依次找字形，西文字体没有汉字 → 中文自动落到宋体，所以"英文在前、中文在后"一行就能实现中英分字体。

### 4.2 整体字号

| 文件 | 位置 | 现值 | 说明 |
|---|---|---|---|
| `github-pro-light.css` | **22 行** `--gp-font-size` | `17px` | light 全局字号开关 |
| `github-pro-dark.css` | **64~67 行** `html { font-size: 17px }` | `17px` | dark 全局字号开关 |
| `base.user.css` | **61 行** `font-size: 17px` | `17px` | 代码块字号（保持与正文一致） |

想再大 1px 就把三处都改成 `18px`；标题、行内代码是相对单位（em），会自动跟着缩放。

### 4.3 左侧文件树（字体 / 字号 / 上下间距）

文件树**没有单独的字体**，继承 `body` → Windows 上中文实际渲染 **微软雅黑**、英文/数字 **Segoe UI**。

- **字号 + 上下间距**（light 已有规则）：`github-pro-light.css` **789 行**：

```css
.file-node-content {
  padding: 2px 8px;   /* 第 1 个值 = 上下间距，第 2 个 = 左右间距 */
  line-height: 1.5;   /* 行高，调大行与行更松 */
  font-size: 13.5px;  /* 字号 */
}
```

- **dark 主题没有这段**（用 Typora 默认约 13px）。想在 dark 里自定义，把上面那段复制到 `github-pro-dark.css` **文件末尾 `@media print` 之前**即可；也可以把它放进 `base.user.css`，两个主题一起管。
- 只想换文件树字体、不动正文：在同一段里加一行
  `font-family: "Microsoft YaHei", sans-serif;`（换成你要的名字）。
- 顶部"文件/大纲"切换页签的字号：light **769 行** `.sidebar-tab { font-size: 13px }`。
- 大纲面板字号：light **833 行** `.outline-item { font-size: 13.5px }`。

### 4.4 直角 vs 圆角

**light 主题（改 3 个变量，全局生效）**：`github-pro-light.css` **57~59 行**

```css
--gp-radius-s: 6px;   /* 小：按钮、引用卡片、编辑面板 */
--gp-radius-m: 10px;  /* 中：表格 */
--gp-radius-l: 12px;  /* 大：代码块 / YAML / TOC / 公式卡片 */
```

改成 `0px` 就是直角。四条边不同圆角写法：`border-radius: 8px 8px 0 0;`（左上 右上 右下 左下）。

**dark 主题是硬编码的**，逐处改（行号见第 7 节索引）：

| 元素 | 行号 | 现值 |
|---|---|---|
| 代码块卡片 `.md-fences` | 1211 | 12px |
| YAML 卡片 | 1267 | 12px |
| TOC 卡片 | 1307 | 12px |
| 公式卡片 | 1375 | 12px |
| 公式编辑面板 | 1386 | 8px |
| 表格外框 | 1397 | 10px |
| 表格四角单元格 | 1417 / 1420 / 1423 / 1426 | 各 10px |
| 引用卡片 | 1443 | 6px |
| 窗口级（侧栏 / 编辑区） | 1509 / 1514 | 10px |

### 4.5 三个小圆点（mac 三色灯）

每个主题有 **3 处**（代码块 / YAML / TOC 各一），规则相同：

```css
.md-fences::before {
  content: "";
  position: absolute;
  top: 11px;      /* 距卡片顶部的距离 */
  left: 14px;     /* ← 距左侧的距离，改这里 */
  width: 12px;    /* 圆点直径 */
  height: 12px;
  border-radius: 50%;
  background: #ff5f56;                    /* 第 1 个（红） */
  box-shadow:
    20px 0 0 #ffbd2e,                     /* 第 2 个（黄）：20 = 与前一个的间距 */
    40px 0 0 #27c93f;                     /* 第 3 个（绿）：40 = 与第 1 个的距离 */
}
```

| 位置 | dark 行号 | light 行号 |
|---|---|---|
| 代码块 `.md-fences::before` | 1215（top 在 1218 / left 在 1219） | 401（top 404 / left 405） |
| YAML `pre.md-meta-block::before` | 1271（top 1274 / left 1275） | 665（top 668 / left 669） |
| TOC `.md-toc::before` | 1313（top 1316 / left 1317） | 706（top 709 / left 710） |

- **改距离**：把 3 处的 `left: 14px` 一起替换（全局搜索替换最稳，这两个文件里 `left: 14px` 只出现在圆点规则中）。
- **改间距**：改 `box-shadow` 里的 `20px` / `40px`（第 3 个的值 = 第 2 个的值 × 2 才均匀）。
- **改大小**：`width` / `height`；**改颜色**：`#ff5f56 / #ffbd2e / #27c93f`。
- 不想要圆点：把三处 `::before` 的 `content: ""` 改成 `content: none`。
- 注意 `top` 的值是配合卡片顶部留白（代码块 `padding-top: 30px`）设计的，卡片留白没动的话 `top` 别改太大，圆点会压到内容。

### 4.6 表格竖线深浅（dark）

- **文件**：`github-pro-dark.css` **41 行** `:root` 里 `--table-grid-color: #454e58;`
- 想更明显 → 更亮一点（如 `#565f6a`）；想更淡 → 靠近背景色（如 `#3d444d`）。
- 斑马纹：**138 行附近** `table tr:nth-child(2n) { background-color: #1e252e }`；行 hover 底色在下方不远处（`rgba(110,118,129,0.14)`）。
- light 主题竖线用的是普通边框色 `--gp-border`（22 行附近定义 `#d0d7de`），想调只影响表格可搜 **533 行** `#write table` 段，把里面的 `var(--gp-border)` 换成固定色值。

### 4.7 mermaid 图表卡片深浅

| 主题 | 位置 | 变量 / 值 |
|---|---|---|
| dark | `:root` **43 行** | `--mermaid-card-bg: #10161d`（越小越深） |
| light | `:root` **77 行** | `--gp-mermaid-bg: #e9edf2`（越大越深，改 `#dfe4ea` 更深） |

### 4.8 公式块卡片

- dark：**1370~1380 行** `#write .mathjax-block .md-rawblock-container, .md-math-container` 段，改 `background-color`（现 `var(--surface-overlay)`，即 `:root` 31 行的 `#1e252e`）、`border`（现 `var(--border-color)` `#3d444d`）、`padding`（内边距，现 `22px 16px`，第一个值上下、第二个左右）。
- light：**970~986 行**，同理，颜色走 `--gp-quote-bg` / `--gp-border`。

### 4.9 水平分割线粗细

- dark：**125~128 行** `hr { height: 1.5px; ... }`
- light：**356~365 行** `hr { height: 3px; ... }`
- 改数字即可（1px 起步，太大就粗了）；颜色在同一段 `background-color`。

### 4.10 TOC 空状态的提示文字

- dark：**1352 行**、light：**749 行**：

```css
content: "目录为空 —— 在正文中插入标题后，这里会自动生成目录";
```

- 换成任何你喜欢的文案；颜色/字号在同一段下面的 `color` / `font-size`。

### 4.11 滚动条

- dark：**552~566 行**，滑块 `::-webkit-scrollbar-thumb { background-color: #454e58 }`（还有 hover/active 两档颜色）。
- light：`:root` **103~104 行** `--gp-scrollbar-thumb` / `--gp-scrollbar-thumb-hover`（半透明灰）。
- 滑块宽度：light `--gp-scrollbar-size`（`#write::-webkit-scrollbar { width: ... }`，约 636 行起）；dark 未指定（系统默认宽）。

### 4.12 行内代码 / 代码块外观

| 改什么 | 文件 | 位置 | 现值 |
|---|---|---|---|
| 行内代码背景 | `base.user.css` | **67 行** | `#f0f0f0`（light 生效；dark 有自己的规则） |
| 行内代码缩放 | `base.user.css` | **66 行** | `0.99em`（可写死如 `14px`） |
| dark 行内代码 | dark | **1449~1456 行** | 未选中 `rgb(30,37,46)` / 选中 `rgb(47,61,100)` |
| 代码块字号 | `base.user.css` | **61 行** | `17px` |
| dark 代码块底色 | dark | `:root` **31 行** `--surface-overlay` | `#1e252e`（代码块、YAML、TOC、公式卡片共用） |

### 4.13 编辑区宽度 / 版心

- `base.user.css` **76 行** `#write { max-width: 85% !important }`（屏幕宽度的百分比，也可写 `900px` 这类固定值）。
- 源代码模式同步：**82 行**。

### 4.14 主题配色总开关

- **dark**：`github-pro-dark.css` `:root` **19~55 行**。常用：

```css
--bg-color: #151b23;          /* 编辑区/整页背景 */
--side-bar-bg-color: #1d242e; /* 左侧文件树背景 */
--text-color: #b8c0cc;        /* 正文文字 */
--surface-overlay: #1e252e;   /* 卡片底色（代码块/YAML/TOC/公式） */
--border-color: #3d444d;      /* 描边 */
--accent-color: #78a7d1;      /* 链接/选中/光标等强调色 */
```

- **light**：`:root` **12~104 行** 全是 `--gp-*` 变量，每个都有中文注释（正文 `--gp-text`、背景 `--gp-bg`、链接 `--gp-link`…）。
- 改 `:root` 变量 = 一处修改全局生效，优先用这种方式。

---

## 5. 怎么自己找到要改的位置

**方法一：搜关键词（最常用）**
用 VS Code 打开主题文件，`Ctrl + F` 搜：
- 改代码块 → 搜 `md-fences`；改目录 → `md-toc`；改表格 → `table`；改引用 → `blockquote`
- 改某个颜色 → 直接搜色值（如 `#454e58`）；改圆角 → 搜 `border-radius`
- 注意一页文件里同名选择器可能出现多次（例如 light 的圆点就有 3 处），看清楚行号上下文再改。

**方法二：浏览器开发者工具精确定位（进阶）**
Typora 就是 Chromium 内核：在编辑器里右键 → `检查元素`（或 `Shift + F12`），像改网页一样点选元素，右侧能看到**生效中的每条规则来自哪个文件哪一行**，还能实时试改。找到满意的值后，回 CSS 文件里正式改。这是"想知道某个东西叫什么"时的终极手段。

**方法三：改之前先记注释**
每改一处就在行尾加 `/* 改过 */` 或日期备注，回溯时一目了然。

---

## 6. 常见坑

1. **改了没生效？** 按顺序查：
   - 忘了 `Ctrl + R` 重新加载；
   - 被更高优先级覆盖：主题里写死的值 vs `base.user.css`（后加载，赢）；带 `!important` 的规则几乎必胜；
   - 偏好设置里的字体设置和主题打架 → 偏好设置保持"默认"；
   - 改的是 `github/base.css`（会被主题覆盖）或改错了 light/dark 其中一个文件。
2. **`!important` 的教训**（真实案例）：旧版 dark 主题表格写了 `border: 0 !important; border-left: 1px solid ...`，`!important` 把**同一规则里后面所有边框一起清零**，竖线直接消失。记住：`border: 0` 这种"整体清零"不要加 `!important`，且要写在其他 border 声明的**前面**。
3. **语法错误会连坐**：少一个分号、少一个 `}`，从出错处到下一个正确结构之间的规则全部失效，表现为"莫名其妙的样式塌掉"。改完刷新发现大片异常，先检查刚改的那几行括号/分号。VS Code 会用红色波浪线提示。
4. **`@media print` 和 `@page` 别乱动**：文件末尾的打印段负责"导出 PDF 白底黑字"，改坏它打印就会出问题；`@page` 必须保持在最外层。
5. **`github/base.css` 不要改**：它是官方底座，你的所有覆盖都写在主题文件里就够了——改底座的话，哪天换主题就忘了这里动过。

---

## 7. 行号总索引

> 行号基于 2026-09-04 交付版本，改动后会偏移，以搜索关键词为准。

### github-pro-dark.css

| 内容 | 行号 |
|---|---|
| `:root` 配色变量（背景/文件树/文字/卡片/边框/强调色） | 19~55 |
| 表格网格线颜色 `--table-grid-color` | 41 |
| mermaid 卡片底色 `--mermaid-card-bg` | 43 |
| 整体字号 `html { font-size }` | 64~67 |
| 分割线 `hr` | 125 |
| 表格斑马纹 | 138 |
| 滚动条滑块颜色 | 552~566 |
| 文件树选中/悬停底色 | 472~483 |
| `body` 字体栈 | 1137 |
| 代码块卡片 + 三圆点 | 1208~1224 |
| YAML 卡片 + 圆点 | 1259~1286 |
| TOC 卡片 + 圆点 | 1299~1322 |
| TOC 空状态提示文字 | 1347~1357 |
| mermaid 卡片 | 1360~1366 |
| 公式卡片（含编辑面板） | 1370~1387 |
| 表格（外框 + 四角圆角） | 1393~1426 |
| 引用卡片圆角 | 1443 |
| 行内代码（未选中/选中） | 1449~1456 |
| 窗口级圆角（侧栏/编辑区） | 1506~1515 |
| 打印/导出规则（勿动结构） | 1517 起 |

### github-pro-light.css

| 内容 | 行号 |
|---|---|
| `:root` 全部 `--gp-*` 变量 | 12~104 |
| 字体栈 `--gp-font-sans` / `--gp-font-mono` | 14~18 |
| 整体字号 `--gp-font-size` | 22 |
| 圆角三变量 `--gp-radius-s/m/l` | 57~59 |
| mermaid 卡片底色 `--gp-mermaid-bg` | 77 |
| 滚动条颜色变量 | 103~104 |
| 行内代码 | 373~380 |
| 分割线 `hr` | 356 |
| 代码块卡片 + 圆点 | 389~413 |
| 表格（圆角走 `--gp-radius-m`） | 533~567 |
| YAML 卡片 + 圆点 | 663~686 |
| TOC 卡片 + 圆点 | 704~718 |
| TOC 空状态提示文字 | 742~755 |
| 文件树 `.file-node-content` | 789~795 |
| 文件树选中/大纲项 | 820 / 833 |
| mermaid 卡片规则 | 957~961 |
| 公式卡片 | 970~986 |
| 打印/导出规则 | 1060 起 |

### base.user.css

| 内容 | 行号 |
|---|---|
| 字体四变量（正文/代码 × 中/英） | 21~33 |
| 正文字体规则 | 37~42 |
| 代码块/行内代码/YAML 字体规则 | 48~55 |
| 代码块字号 | 60~62 |
| 行内代码背景/字号 | 65~70 |
| 编辑区宽度 `max-width` | 75~79 |
| 源代码模式宽度 | 81~83 |

---

## 8. 改坏了怎么恢复

1. **有备份**：用备份文件夹里的对应文件覆盖回去（这就是第 1 节让你先备份的原因）。
2. **没备份**：可以再找我要一份原始文件（重新下载一遍后备份一份，再继续修改），或者：
   - 只想撤销最近一小段 → 在 VS Code 里 `Ctrl + Z` 连按几次；
   - 完全乱了 → 删掉主题 css，重装官方 github 主题，再把 `base.user.css` 单独恢复。
3. **判断"改坏了"还是"本来就这样"**：把刚改的部分整段注释掉（行首加 `/*`、段尾加 `*/`）再刷新——恢复正常就说明是这次改的，没恢复就是别处的问题。
